package com.sist.action;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashMap;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.sist.dao.LocationDAO;
import com.sist.vo.LocationVO;
public class showLocAction implements SistAction {
	
	@Override
	public String proRequest(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		
		LocationDAO dao = new LocationDAO();
		ArrayList<LocationVO> list = dao.listAll();
		HashMap<String,ArrayList<String>> district = new HashMap<String,ArrayList<String>>();
		
		for(LocationVO l : list) {
			ArrayList<String> arr = new ArrayList<String>();
			district.put(l.getDistrict(), arr);
		}
		for(LocationVO l : list) {
			String dong = l.getDong();
			district.get(l.getDistrict()).add(dong);
		}
		HashMap<String, HashMap<String,ArrayList<String>>[]> location = new HashMap<String, HashMap<String,ArrayList<String>>[]>();
		
		
		return "showLoc.jsp";
	}
	
}
