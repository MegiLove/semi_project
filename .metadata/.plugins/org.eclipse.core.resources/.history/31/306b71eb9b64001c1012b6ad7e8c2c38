package com.sist.action;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashMap;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.sist.dao.LocationDAO;
import com.sist.vo.LocationVO;
public class showLocAction implements SistAction {
	
	@Override
	public String proRequest(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		
		LocationDAO dao = new LocationDAO();
		ArrayList<LocationVO> list = dao.listAll();
		HashMap<String,ArrayList<String>> provinces = new HashMap<String,ArrayList<String>>();
		HashMap<String,ArrayList<String>> districts = new HashMap<String,ArrayList<String>>();
		print(dao.getProvince());
		for(LocationVO l : list) {
			System.out.println(l.getDong());
			ArrayList<String> emptyArr = new ArrayList<String>();
			provinces.put(l.getProvince(), emptyArr);
			ArrayList<String> emptyArr2 = new ArrayList<String>();
			districts.put(l.getDistrict(), emptyArr2);
		}
		
		for(LocationVO l : list) {
			provinces.get(l.getProvince()).add(l.getDistrict());
			districts.get(l.getDistrict()).add(l.getDong());
		}
		
		ArrayList<String> ord_provinces = new ArrayList<String>();
		for(LocationVO l : list) {
			if(ord_provinces.size()==0) {
				ord_provinces.add(l.getProvince());
			}
			else if(!ord_provinces.get(ord_provinces.size()-1).equals(l.getProvince())) {
				ord_provinces.add(l.getProvince());
			}
		}
		
		System.out.println(districts.toString());
		
		request.setAttribute("provinces", provinces);
		request.setAttribute("districts", districts);
		request.setAttribute("ord_provinces", ord_provinces);
		
		
		return "showLoc.jsp";
	}
	
}
